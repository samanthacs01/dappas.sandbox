import { Button } from '@workspace/ui/components/button';

const BuyButton = () => {
  return (
    <Button
      variant={'outline'}
      className="w-72 rounded-none font-normal py-2.5 border-black"
      onClick={() => {}}
    >
      Buy now
    </Button>
  );
};

export default BuyButton;
