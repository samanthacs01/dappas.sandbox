export type ObjectDimensions = {
  width: number;
  height: number;
  depth: number;
};

export type TextZone = {
  text: string;
  position: { x: number; y: number };
  fontSize: number;
  color: string;
};
